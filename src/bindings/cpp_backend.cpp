#include <vector>
#include <math.h>

using namespace std;

class Signal {
public:
    vector <double> square(double amplitude, uint32_t frequency, double duration, double dt = 0.01){
        vector <double> signal;
        for(double t=0; t<=duration; t+=dt){
            signal.push_back((int)sin()==-1?0:1)
        }
    } 
    vector <double> square(double amplitude, uint32_t frequency, double duration, double dt = 0.01){
        vector <double> signal;
        
    } 
    vector <double> square(double amplitude, uint32_t frequency, double duration, double dt = 0.01){
        vector <double> signal;
        
    } 

};

